import React, { ReactNode } from 'react'

const uploadAndDashboardLayout = ({children}:{children:ReactNode}) => {
return (
    <main className=''>
        {children}
    </main>
);
};

export default uploadAndDashboardLayout